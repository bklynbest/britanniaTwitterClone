<%= form_for(@breet) do |f| %>
  <% if @breet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@breet.errors.count, "error") %> prohibited this breet from being saved:</h2>

      <ul>
      <% @breet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :breet %><br>
    <%= f.text_area :breet %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
